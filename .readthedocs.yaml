version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py

# Add this section to tell it where the files are
formats: all

python:
  install:
    - requirements: docs/requirements.txt